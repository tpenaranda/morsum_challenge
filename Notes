#Requirements:

- PHP >= 5.6
- Apache >= 2.4
- Composer >= 1.1

#Installation steps:

1. Check 'AllowOverride' on Apache configuration in order to support parsing of .htaccess file.

    <Directory /project_root/>
        AllowOverride All
    </Directory>

2. Do 'composer install'.

3. Configure database at config/config.php.

4. [Optional] Create/seed books table running `php <project_root>/app/seeds/Books.php` (warning this will destroy old data).

5. [Optional] Create/seed vinils table running `php <project_root>/app/seeds/Vinils.php` (warning this will destroy old data).
