Requirements:
- PHP >= 5.6
- Apache >= 2.4

Installation steps:

1. Check 'AllowOverride' on Apache configuration in order to support parsing of .htaccess file.

    <Directory /project_root/>
        AllowOverride All
    </Directory>

2. Configure database at config/config.php. Create/seed books table running `php <project_root>/app/seeds/Books.php` (warning this will destroy old data).
